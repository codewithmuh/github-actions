# name: Manual Workflow Dispatch

# on:
#   workflow_dispatch:
#     inputs:
#       environment:
#         description: 'Choose environment'
#         required: true
#         default: 'staging'

# jobs:
#   manual-job:
#     runs-on: ubuntu-latest
#     steps:
#       - run: echo "🚦 Manual run for ${{ github.event.inputs.environment }} environment"
