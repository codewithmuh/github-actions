# name: Cleanup Old Artifacts

# on:
#   schedule:
#     - cron: '0 3 * * *'  # every day at 3 AM UTC

# jobs:
#   cleanup:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Delete artifacts older than 7 days
#         uses: stefanzweifel/delete-artifacts@v2
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           days: 7
